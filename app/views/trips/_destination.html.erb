<div class="border rounded p-4 mb-2">
  <h3 class="font-semibold"><%= destination.name %></h3>
  <p class="text-sm text-gray-600"><%= destination.city %>, <%= destination.country %></p>
  <div class="mt-2">
    <%= link_to 'View', trip_destination_path(destination.trip, destination), class: 'btn-secondary' %>
    <%= link_to 'Edit', edit_trip_destination_path(destination.trip, destination), class: 'btn-secondary' %>
  </div>
</div>